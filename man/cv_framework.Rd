% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-cv.R
\name{cv_framework}
\alias{cv_framework}
\title{Generic cross-validation framework}
\usage{
cv_framework(
  Y,
  grid,
  n.folds,
  criteria,
  quantile.value,
  fit_fun,
  reconstruct_fun,
  return.models = TRUE,
  verbose.cv = TRUE,
  seed = NULL,
  grid.label = "Degrees of freedom: ",
  model.prefix = "df_idx"
)
}
\arguments{
\item{Y}{The \eqn{(N \times T)} matrix of functional data.}

\item{grid}{Numeric vector of hyper-parameter values to cross-validate.}

\item{n.folds}{Number of folds to be used on cross validation.}

\item{criteria}{Criteria used to divide the data. Valid values are \code{'rows'} or \code{'points'}.}

\item{quantile.value}{The quantile considered.}

\item{fit_fun}{Function \code{(Y.train, grid.value, train.idx)} returning a fitted model. \code{train.idx} contains the training row indices when \code{criteria='rows'} and is NULL otherwise.}

\item{reconstruct_fun}{Function \code{(model, Y.test, train.idx)} returning the predicted \eqn{(N \times T)} matrix used to score the fold.}

\item{return.models}{Should the list of all the models built be returned?}

\item{verbose.cv}{Boolean indicating verbosity of the cross-validation process.}

\item{seed}{Seed for the random generator number (used to build the folds).}

\item{grid.label}{Label prefixing the grid value in verbose messages.}

\item{model.prefix}{Prefix used to name the stored models (\code{'<prefix>=<i>.fold=<j>'}).}
}
\value{
A list containing the error matrix, the execution time, the grid, the criteria and the list of models.
}
\description{
Inner engine shared by all the cross-validation drivers: splits the data into folds, loops over a hyper-parameter grid fitting a model per (grid value, fold) pair via \code{fit_fun}, reconstructs the test predictions via \code{reconstruct_fun}, and collects the quantile error matrix.
}
