% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tensor.R
\name{build_tensor_matrix_sparse}
\alias{build_tensor_matrix_sparse}
\title{Build Sparse Tensor Design Matrix}
\usage{
build_tensor_matrix_sparse(scores, Y.mask, spline.basis)
}
\arguments{
\item{scores}{A matrix of functional Principal Component Analysis (fPCA) scores for each subject.}

\item{Y.mask}{A logical mask matrix of dimensions (N x T) indicating which time points are observed (\code{TRUE}) or missing (\code{FALSE}) for each subject.}

\item{spline.basis}{The evaluation matrix of the B-spline basis functions at the time points.}
}
\value{
A \code{matrix.csr} sparse matrix containing the intercept column, the intercept bases and the scaled spline bases for all valid observations.
}
\description{
Inner function to construct the tensor product design matrix as a sparse \code{SparseM::matrix.csr}, exploiting the local support of the B-spline basis (each row has only a handful of non-zero entries). The column layout is identical to \code{cbind(1, build_tensor_matrix(...))}, i.e. it includes the leading global intercept column, so the result can be passed directly to \code{quantreg::rq.fit.sfn}.
}
