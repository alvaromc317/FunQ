# Check that a parameter is a non-negative number

Check that a parameter is a non-negative number

## Usage

``` r
check_nonnegative_number(x, name)
```

## Arguments

- x:

  The value to check.

- name:

  The parameter name used in the error message.

## Value

No return
